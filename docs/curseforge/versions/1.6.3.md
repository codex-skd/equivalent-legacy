<h2>v1.6.3 - Regalia Slots API Compatibility Fixes</h2>

<h3>Fix</h3>
<ul>
<li><strong>Amulets/Gems couldn't be placed in Regalia Slots API amulet slot</strong>: added <code>gem_of_eternal_density</code> (Gem of Eternal Density) and <code>mind_stone</code> (Mind Stone) to the <code>curios:necklace</code> tag; removed <code>gem_of_eternal_density</code> from the <code>curios:ring</code> tag since it's an amulet.</li>
<li><strong>Repair Talisman didn't repair items in Regalia Slots API slots</strong>: when placed in belt slot, it now repairs both the player's inventory AND items equipped in Regalia slots (necklace, ring, belt, etc.).</li>
</ul>

<hr>

<p><strong>JAR</strong>: <code>equivalent_legacy-26.2-neoforge-26.2.0.57-1.6.3.jar</code></p>

<p><strong>Tag</strong>: <code>26.2-neoforge-1.6.3</code></p>